/**
 * Created by AWITTROCK on 8/25/2017.
 */

import userRoutes from './routes/user';

export default ({
    routes: userRoutes,
    config: {
        validators: {
            LoggedIn: request => true
            //LoggedIn: request => ({code: 401, message: "You must be logged in to access this endpoint"})
        }
    }
});
