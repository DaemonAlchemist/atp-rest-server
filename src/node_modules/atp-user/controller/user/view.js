/**
 * Created by AWITTROCK on 8/25/2017.
 */

import users from '../../data/users';

export default (req, res) => {
    res.send(JSON.stringify(users.filter(user => user.userName === req.params.userName)));
}