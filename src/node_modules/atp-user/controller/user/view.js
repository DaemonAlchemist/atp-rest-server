/**
 * Created by AWITTROCK on 8/25/2017.
 */

import User from '../../model/user';

export default (req, res) => {
    //TODO:  Validate => logged in, has permission, valid id, user exists
    new User().getByUserName(req.params.userName, (err, user, field) => {
        res.send(JSON.stringify({results: user}));
    });
}
